#include <stdio.h>

/* argc is character count and argv is array strings */
int main (int argc, char *argv[])
{
    /* returns the name of the command */
    printf("%s \n", argv[0]);

    return 0;
}

